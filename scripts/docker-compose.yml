services:
  stagnacarta:
    image: nginx:alpine
    container_name: stagnacarta
    restart: unless-stopped
    ports:
      - "8580:80"
    volumes:
      - /volume2/Stagnacarta-public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
